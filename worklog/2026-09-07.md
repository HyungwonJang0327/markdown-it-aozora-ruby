# 2026-09-07 — Phase 1 코어 구현 완료

## 참여 에이전트

- 컨트롤러 (Opus): 태스크 브리프 작성·디스패치·룰링·ledger 관리 (subagent-driven-development)
- implementer ×7 (Sonnet/Haiku): 매처·core ruler·렌더러·통합 테스트·stripRuby 구현 + fix round들
- reviewer ×9 (Sonnet/Haiku, 최종은 Opus): 태스크별 스펙·품질 리뷰 + 스코프 재리뷰 + 브랜치 전체 최종 리뷰

## 논의 내용

- 브랜치 `feat/phase1-core`에서 mvp-todo Phase 1을 태스크당 "implementer → 리뷰 → fix → 재리뷰" 루프로 진행. 태스크 4건이 fix round 1회씩을 거침(전부 테스트 품질·커밋 위생 지적, 동작 결함 0).
- Task 1 최초 디스패치가 세션 한도로 중단 → 새 implementer가 미커밋 작업물을 이어받아 완료. 이전 에이전트가 추가한 `@rolldown/binding-darwin-arm64` devDep은 CLAUDE.md §6 위반으로 되돌림.
- Task 4에서 src 변경이 test 커밋에 혼입 → 히스토리 재작성으로 refactor/test 분리 (a7e9df5 → 1e5e575+80755f3).
- 최종 브랜치 리뷰(/review, opus)가 high 1건 발견: 매처가 닫히지 않는 베이스 run에서 O(n²) (5만 자 → 4.3초, untrusted 입력 DoS 벡터). low 4건(테스트 감지력·주석·한자 혼용).
- 사용자가 5건 전부 승인 → fix wave 3커밋으로 적용. 가드 후 5만 자 <1ms.

## 결론·결정

- Phase 1 (mvp-todo 항목 1~6) 완료: 14커밋, 66 tests, src/ 커버리지 100% (Stmts/Branch/Funcs/Lines), high 0건.
- 매처 인터페이스: `findAozoraRuby(text): RubyMatch[]` (`{index, length, base, reading}`) — rule.ts·strip.ts가 공유 소비.
- 한자 클래스 `[\p{Script=Han}々〆ヶ]` 확정 (ヶ는 Katakana라 명시 포함, `一ヶ月《いっかげつ》` 테스트로 검증) — architecture.md의 "구현 시 테스트로 검증" 항목 이행.
- `𠮟る《しかる》` 원문은 미매치가 정답 (る 비한자 — 아오조라 규칙·Justdown 정규식과 일치). BMP 밖 검증은 `𠮟《しか》る`·`｜𠮟る《しかる》`로 수행.
- stripRuby는 findAozoraRuby 재사용 — 루비로 매치되는 부분만 base로 치환, 매치 안 되는 `《》`·`｜`는 원문 유지 (Justdown의 무차별 `《…》` 제거와 의도적으로 다름 — 렌더 결과 플레인 텍스트와 일치 목적).
- null 입력 런타임 가드 없음 — 공개 시그니처 `string`, throw 금지 규칙은 매치 실패 시 원문 통과 의미로 해석 (필요 시 v1.1).
- 매처에 `《` 부재 시 조기 반환 가드 추가 (O(n²) 완화, 정규식 재작성 없이).
- 도달 불가 방어 분기 대신 non-null 단언 + 정규식 불변식 주석 방식 채택.

## 미결·다음

- Phase 2 (mvp-todo): README 3종, GitHub 저장소·CI, 빌드 산출물 검증, changeset → v1.0.0 publish.
- PR 생성 여부 사용자 확인 대기 (원격 저장소 미생성 상태 — Phase 2 항목과 순서 조율 필요).
- deferred minor (머지 전 불필요 판정): markdown-it 15 타입 workaround 사유 미기록, getInlineChildren 헬퍼 첫 inline만 검사, strip named export 테스트 목적 혼재 등 — 필요 시 Phase 2에서.
- SDD 작업 workspace(.superpowers/sdd/mvp-todo/)는 브랜치 통합 후 삭제 예정.
