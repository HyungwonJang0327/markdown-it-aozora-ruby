# INTENT — markdown-it-aozora-ruby

- **문제**: 아오조라 문고식 루비 표기(`漢字《かんじ》`, `｜ベース《よみ》`)를 지원하는 markdown-it 플러그인이 npm에 없다. Justdown 앱에 검증된 파서가 있지만 앱 안에 갇혀 있다.
- **목표**: 파서를 독립 패키지로 분리해 npm에 `v1.0.0` 배포하고, CI(lint·typecheck·test·build)가 그린인 상태를 만든다.
- **비목표**: 픽시브 `[[rb:]]` 등 타 표기법 지원(내부 구조만 확장 가능하게 유지), 구분자 커스터마이즈 옵션 — 2~3일 범위를 지키기 위해.
- **사용자 시나리오**: ① 사용자가 `md.use(aozoraRuby)` 후 `漢字《かんじ》`를 렌더한다 → `<ruby>漢字<rt>かんじ</rt></ruby>`가 출력된다. ② 사용자가 `stripRuby('｜東京《とうきょう》')`를 호출한다 → `東京`을 얻는다. ③ 코드 스팬 안의 `《》`는 변환되지 않는다.
- **성공 지표**: `src/` 테스트 라인 커버리지 95% 이상.
- **제약**: 기간 2~3일. TypeScript·tsup·vitest 고정. 런타임 의존성 0 (markdown-it은 peerDependency).
- **포트폴리오 관점**: 오픈소스 패키지의 설계→테스트→문서(en/ja/ko)→배포 전 과정을 완수할 수 있음을 증명.
- **미결**: Justdown이 이 패키지를 의존성으로 교체할지 (별도 검증 후 결정, decisions/에서 추적).
